import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App' 
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)
4.  **CSS 파일**: `src/index.css` 파일이 없으면 에러가 날 수 있으니, 빈 파일이라도 만들어 두세요.
